import React from 'react';
import DeathRegistrationForm from '../../components/DeathRegistrationForm';

const RegisterDeathPage: React.FC = () => {
  return (
              <DeathRegistrationForm/>
)
    ;
};

export default RegisterDeathPage;
